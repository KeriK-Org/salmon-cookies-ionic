import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Salmon Cookies',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
